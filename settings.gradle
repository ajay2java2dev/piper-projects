pluginManagement {
    repositories {
        mavenCentral()
        google()
        mavenLocal()
        maven { url 'https://repo.spring.io/release' }
    }
}

rootProject.name = 'piper-projects'
include 'piper-resume-service'
